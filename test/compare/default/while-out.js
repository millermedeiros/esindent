
// WhileStatement

while (n--) {
  // nested
  while (i++) {
    // moar nested
    while (z++ < 0) {
      // inception
      foo();
      while (j++) {
        // deeper
        bar();
      }
    }
  }
}

